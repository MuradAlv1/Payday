server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/stocks
    password: postgres
    username: postgres

  jpa:
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: kafka:9092

stock-service:
  stocks-topic-name: stocks
